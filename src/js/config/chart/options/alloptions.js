define([],

function() {
  return function(options, preferences) {
    preferences = preferences || {};
/*
    options.push({
      name : "colors",
      group : "general",
      weight : -1000,
      editors : [{
        type : "colorlist",
        options : {
          default : "#aaa,#bbb,#fa9,#9af"
        }
      }]
    });

    options.push({
      name : "color",
      group : "general",
      weight : -1000,
      editors : [{
        type : "color",
        options : {
          default : "#fa9"
        }
      }]
    });
*/
  };
});